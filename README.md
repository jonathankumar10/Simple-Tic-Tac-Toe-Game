# Simple-Tic-Tac-Toe-Game

Created and implemented a simple Tic Tac Toe game which can be run through the command line.

To run the game, clone the git repo and enter the following command in the game's directory: python Main.py